name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Update repository
        run: |
          cd /home/pouyam/Desktop/waste_syndicate
          git pull origin main

      - name: Deploy
        run: |
          cd /home/pouyam/Desktop/waste_syndicate
          docker compose up -d --build
